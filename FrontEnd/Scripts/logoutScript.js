function signOut() {
    // Here you can include logic to perform sign-out actions
    // For now, just return true to indicate that the sign-out request should be sent to the server
    return true;
}

function redirectToPage(page) {
    window.location.href = page;
}
